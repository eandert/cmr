"""Helpers for the canonical v2v4real_inputs/ tree.

`scripts/build_v2v4real_input_bucket.py` is the user-facing CLI; this package
holds the shared logic so each phase (manifest, readme, import, gpem refit)
is independently testable.
"""
